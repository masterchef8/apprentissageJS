/**
 * Created by Somebody on 20/05/15.
 */

var textValue = document.getElementById('text').value;
var text = document.getElementById('text');

text.addEventListener('focus', check(textValue));

function check(valeur){
    if (valeur > 0 && valeur >= ){

    }
}